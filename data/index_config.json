{
  "version": "1.0",
  "last_updated": "2026-01-18T00:00:00Z",
  "updated_by": "system",
  "description": "Configuration for dual-CMO system. Controls which folders are indexed in which search index.",
  "folders": {
    "data/Train_CMO/Artists_Way": {
      "indexes": ["private"],
      "enabled": true,
      "description": "Artist's Way course materials - PRIVATE CMO ONLY (testing configuration)"
    },
    "data/Train_CMO/Business_Growth": {
      "indexes": ["private"],
      "enabled": true,
      "description": "Business Growth content - PRIVATE CMO ONLY (testing configuration)"
    },
    "data/Train_CMO/Hero_Journey": {
      "indexes": ["public"],
      "enabled": true,
      "description": "Hero's Journey materials - PUBLIC CMO (testing configuration)"
    },
    "data/Train_CMO/Sales_Pitches": {
      "indexes": ["public"],
      "enabled": true,
      "description": "Internal sales pitches - PUBLIC CMO (testing configuration)"
    },
    "data/Train_CMO/FloDesk_Emails": {
      "indexes": ["public"],
      "enabled": true,
      "description": "FloDesk email campaigns - PUBLIC CMO (testing configuration)"
    },
    "data/Train_CMO/Skool_Community": {
      "indexes": ["public"],
      "enabled": true,
      "description": "Skool community content - PUBLIC CMO (testing configuration)"
    },
    "data/Train_CMO/21_DOMA": {
      "indexes": ["public"],
      "enabled": true,
      "description": "21 Days of Marketing Attraction - PUBLIC CMO (testing configuration)"
    }
  },
  "indexes": {
    "private": {
      "name": "gptkbindex-internal",
      "description": "Private CMO - Currently Artists_Way + Business_Growth only (will be changed via admin UI later)",
      "document_count": 0,
      "last_indexed": null
    },
    "public": {
      "name": "gptkbindex-public",
      "description": "Public CMO - Currently all other content (will be changed via admin UI later)",
      "document_count": 0,
      "last_indexed": null
    }
  },
  "validation": {
    "allowed_file_types": [".pdf", ".docx", ".txt"],
    "max_file_size_mb": 100
  },
  "notes": [
    "This is a TESTING configuration with reversed typical permissions.",
    "Private CMO has LESS data (Artists_Way + Business_Growth).",
    "Public CMO has MORE data (everything else).",
    "This will be reconfigured later via the admin portal UI.",
    "Non-technical admins will use the admin portal to change folder assignments."
  ]
}
